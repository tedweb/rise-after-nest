{
  "name": "rise-after-nest",
  "private": true,
  "workspaces": [
    "astro-frontend",
    "sanity-backend"
  ],
  "scripts": {
    "dev:site": "npm run dev --workspace astro-frontend",
    "dev:studio": "npm run dev --workspace sanity-backend",
    "build": "npm run build --workspace sanity-backend && npm run build --workspace astro-frontend",
    "validate:content": "npm run validate --workspace sanity-backend",
    "seed:content": "npm run seed:import --workspace sanity-backend"
  }
}
